<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  GenCall v2 - Loop Campaign UAC (outbound originate leg) - design §4.1/§4.2.

  Built on SIPp's proven built-in 'uac' skeleton (validated on sipp 3.6.1) so the
  INVITE/200/ACK/BYE transaction matching is exactly what SIPp expects. The A/B
  number pair is injected from the campaign -inf CSV:

      [field0] = a_number (calling)   [field1] = b_number (called)

  Per-call <log> lines (-trace_logs) feed the CallRecord tail-parser
  (gencall/core/call_records.py). A-side duration = t_bye_sent - t_200ok_received.

  IMPORTANT — all <log> actions live INSIDE <recv> elements. A standalone <nop>
  between the INVITE <send> and its response <recv>s makes sipp 3.6 treat the
  incoming 180 as "unexpected" (it races the nop) and abort the call, so there
  are no nops here. t_bye_sent is logged when the 200-to-BYE arrives (one RTT
  after the BYE leaves — sipp <send> cannot carry an action).

  Hold duration: a bare <pause/> uses SIPp -d (set by the LoopEngine from the
  campaign duration_s); SIPp does not substitute -inf fields inside a pause
  attribute, so [field2] cannot be used there.
-->
<scenario name="GenCall - Loop UAC">

  <send retrans="500">
    <![CDATA[
      INVITE sip:[field1]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "[field0]" <sip:[field0]@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[field1]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:[field0]@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=GenCall 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=GenCall Loop
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 8 0 101
      a=rtpmap:8 PCMA/8000
      a=rtpmap:0 PCMU/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-15
      a=ptime:20
      a=sendrecv
    ]]>
  </send>

  <!-- Provisional responses are optional. -->
  <recv response="100" optional="true" />
  <recv response="180" optional="true" />
  <recv response="183" optional="true" />

  <!-- 200 OK received → A-side clock STARTS (t_200ok_received). -->
  <recv response="200" rtd="true" crlf="true">
    <action>
      <log message="loop_uac direction=out call_id=[call_id] a_number=[field0] b_number=[field1] event=answer t_200ok_received=[clock_tick] final_code=200" />
    </action>
  </recv>

  <!-- ACK the 2xx. To-tag echoed via [peer_tag_param] (the proven built-in form). -->
  <send>
    <![CDATA[
      ACK sip:[field1]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "[field0]" <sip:[field0]@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[field1]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: <sip:[field0]@[local_ip]:[local_port];transport=[transport]>
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- Hold the call for -d milliseconds (LoopEngine sets -d from duration_s). -->
  <pause />

  <!-- BYE ends the call. -->
  <send retrans="500">
    <![CDATA[
      BYE sip:[field1]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "[field0]" <sip:[field0]@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[field1]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- 200 to the BYE → A-side clock STOPS (t_bye_sent, +1 RTT). -->
  <recv response="200" crlf="true">
    <action>
      <log message="loop_uac direction=out call_id=[call_id] a_number=[field0] b_number=[field1] event=bye t_bye_sent=[clock_tick]" />
    </action>
  </recv>

  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200" />
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000" />

</scenario>
