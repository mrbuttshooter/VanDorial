# Python
gencall/**/__pycache__/
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Runtime data / secrets (never commit)
*.db
*.sqlite
*.log
.env
gencall/logs/
gencall/media/*.wav

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Built NOC console — COMMITTED on purpose so native/offline installs (which
# have no Node) serve the UI. The Docker build regenerates it (stage 1).
# gencall/web/console/   <- intentionally tracked

# Offline wheelhouse + release zips (build artifacts, never commit)
vendor/wheelhouse/
*.zip

# Tooling / editor
.playwright-mcp/
.DS_Store
*.local
